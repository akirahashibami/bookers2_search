<div class="row">
<div class="col-xs-12">
  <h2>Follower Users</h2>

  <table class="table">

    <thead>
      <tr>
        <th>image</th>
        <th>name</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
      <tr>
        <td><%= attachment_image_tag(user.followed, :profile_image, :fill, 50, 50, fallback: "no_image.jpg") %></td>
        <td><%= user.followed.name %></td>
        <td><p>フォロー数 : <%= user.followed.follower.count %></p></td>
        <td><p>フォロワー数 : <%= user.followed.followed.count %></p></td>
        <td><%= link_to "Show", user_path(user.followed.id), class: "user_#{user.id}" %></td>
      </tr>
      <% end %>
    </tbody>

  </table>
</div>
</div>
